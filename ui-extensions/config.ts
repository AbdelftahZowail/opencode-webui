/**
 * Extension enable list — the one place to turn extensions on/off.
 * Add or remove an id here; the change applies instantly via HMR,
 * no code edits and no reload needed.
 */
export const enabled: string[] = ["report"];
