<!-- Tip: run `/report` in the app — it prefills this template automatically
with a diagnostics bundle (optionally `/report --agent` to have the session
agent file the issue via the gh CLI). File by hand only when the app itself
won't load. -->

---
name: Bug report
about: Something in the OpenCode webui is broken or wrong
title: "WebUI bug: "
labels: ["bug"]
---

### What happened

### Steps to reproduce

1.

### Expected

### Actual

### Environment

- webui version:
- browser:
- OS:

### Diagnostics

If you ran `/report`, paste the diagnostics JSON block here; otherwise fill in
what you can (webui version is in the console boot log).

```json
(paste diagnostics here)
```
