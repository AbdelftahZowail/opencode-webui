/**
 * Extension entry point — the single wiring file.
 * To add an extension: create ui-extensions/<name>/ and import it here.
 * To remove one: delete its import line (and the folder).
 * See ui-extensions/README.md.
 */

import "./runtime-status";
