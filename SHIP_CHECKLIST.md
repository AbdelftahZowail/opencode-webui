# Ship Checklist — v1.0 SHIPPED 2026-09-01

> All must-have items landed and verified. Remaining ship-day steps are the two
> commands at the bottom (npm publish is deliberately the user's).

## Must-have — DONE ✅

- [x] **Auth for public domains** — `server/auth.ts` (501 lines)
  - `WEBUI_PASSWORD` or 6-word generated passphrase (~42 bits), printed once; wildcard binds refused without a password (message names the env var); default bind 127.0.0.1.
  - SHA-256 digest + `crypto.timingSafeEqual` (plaintext never stored/logged); HMAC session tokens, 30d, secret persisted at `~/.local/state/opencode-webui/secret.key` (0600) so sessions survive restarts.
  - `GET /login` (JS-free dark page, `X-Frame-Options: DENY`, CSP, noindex) + JSON/form `POST /api/auth/login`; `GET /api/auth/logout`.
  - Middleware gates everything (API JSON 401, pages 302→`/login?next=` sanitized, SSE, WS upgrades); 5/min/IP rate limit with `Retry-After`.
  - DNS-rebinding: Host validated every request (loopback, `WEBUI_HOST`, `X-Forwarded-Host`); Origin checked on POST/PUT/DELETE/PATCH.
  - Verified: 73/73 auth-matrix tests green + live-proxy curl smoke.
- [x] **First-boot banner** — URL, password (only when generated), TUI-session line, extension-dir hint, skill path. Matches README verbatim.
- [x] **One-command package** — `private:false`, v1.0.0, `bin` → `server/index.ts` (shebang on line 1), `files: ["dist","server","skills","ui-extensions/README.md"]`, `prepublishOnly: typecheck && gen:skill && build`, LICENSE added. `npm pack --dry-run`: 16 files, 1.9 MB.
- [x] **Phase C user extension dirs** — `server/userExtensions.ts`: `~/.config/opencode/webui-extensions/<name>/main.tsx` + `<project>/.opencode/webui-extensions/` → same manifest/bundle pipeline as plugin UIs (mtime cache-bust, id-collision skip+warn). Zero frontend changes.
- [x] **Extension contract check** — `scripts/uitest/extensions-check.ts`: hot-add → register → edit hot-swap (zero reloads, bootId sentinel) → delete → prune (reload-free, Vite 8) → user-dir manifest/bundle, driven through headless Chrome CDP on an isolated instance. 7/7 PASS, self-cleaning.
- [x] **`/report` built-in** — `ui-extensions/report/` (aliases: `bug`): scrubbed diag bundle (error ring, version/repo via `GET /api/webui/config`, UA, ext ids) → prefilled GitHub issue; clipboard fallback; `--agent` files via the session agent's `gh` through `sendPromptTo`. `.github/ISSUE_TEMPLATE/bug_report.md` mirrors it.
- [x] **Agent skill (context injection)** — `skills/webui/SKILL.md` generated by `scripts/gen-skill.ts` from `ui-extensions/README.md` (deterministic); auto-synced to `~/.config/opencode/skills/webui/` on every boot + `--install-skill`. Its description is the always-injected line making every opencode agent aware of the webui/extensions/`/report`.
- [x] **Prebuilt binaries** — embed dist/ via generated asset manifest + `Bun.file` shim (adjacent dist is impossible inside `--compile` binaries — virtual FS). 5 targets, tar.gz + SHA256SUMS; embedded agent skill syncs in binaries too. Verified boot-from-empty-dir end-to-end.
- [x] **Release CI** — `.github/workflows/release.yml`: tag `v*` → typecheck+build gate → 5-target matrix → `gh release create` with artifacts + checksums.
- [x] **Reverse-proxy docs** — `docs/reverse-proxy.md` (Caddy/nginx, `X-Forwarded-Proto`, ws upgrade, SSE-safe timeouts, env table).
- [x] **README rewrite** — `bunx` quickstart with the banner block, binaries, security model, skill, extensions, `/report`, dev section.
- [x] **Docs truth pass** — kind count 11; skill path `skills/webui/`; dev-sandbox removal documented; folder-deletion is reload-free (Vite 8) corrected in AGENTS.md + ui-extensions/README.md.

## Ship-day remainder (deliberately the user's)

```sh
npm login                      # once, as AbdelftahZowail
npm publish --access public    # package.json is publish-ready (1.0.0)
```

Tag v1.0.0 + push (done at ship time) triggers the release workflow: binaries attach to the GitHub Release automatically.

## Ongoing (habit, not task)

- [ ] `bun run check:swap` after every engine bump — recorder → cursor-replay swap is the one open streaming item.

## Already done (not in this list)

- Extension kinds: 11 (`region`/`command(+keybind)`/`slash`/`message`/`message.decoration`/`message.part`/`contextMenu`/`hook`/`page`/`settings`/`tool.renderer`) + `notify` bridge.
- Region markers (17 via `bun run regions`), per-item `SlotErrorBoundary`, ancestry gating, prune/hot add-edit-remove.
- Runtime plugin UI pipeline + per-id kill switch (Settings › Extensions).
- HMR coalescing, watcher ignores for `*.md`.

## Claim corrections (2026-08-31, kept so they don't rot back)

- Kind count is 11, not 9 (`contextMenu`, `message` were missing).
- Skill path is `skills/webui/SKILL.md` — `.opencode/skills/webui-dev/` never existed in this tree.
- `dev-sandbox` was REMOVED from `ui-extensions/` (only `runtime-status/` + `report/` ship); the old "`/ext/dev-sandbox` renders" verification claim was false — replaced by the contract check.
- (2026-09-01) Deleting an extension folder no longer needs the coalesced reload the docs claimed — Vite 8 prunes at the HMR boundary; proven by the contract check's Phase C.
